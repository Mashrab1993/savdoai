# SAVDOAI v25.3 — Railway Config
# Build: har bir servis uchun Railway dashboardda Dockerfile path ko'rsating:
#   savdoai-bot  → services/bot/Dockerfile
#   savdoai-api  → services/api/Dockerfile
#   savdoai-web  → Dockerfile.web
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.web"
