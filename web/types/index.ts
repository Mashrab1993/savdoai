/**
 * Shared types. Re-export from mock-data for now; replace with API DTOs when wired.
 */

export type {
  Client,
  Product,
  Debt,
  Invoice,
  InvoiceItem,
  Activity,
  Apprentice,
  ApprenticeExpense,
  Expense,
  PriceGroup,
  CashTransaction,
} from "@/lib/mock-data"
